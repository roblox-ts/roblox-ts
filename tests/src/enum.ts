// Basic enum test
export enum Fruits {
	Apple,
	Orange,
	Pear,
}

// Numeric enum test
export enum Breads {
	White = 5,
	Wheat,
	Pumpernickel,
}

// String enums
export enum Soups {
	Tomato = "TOMATO",
	ChickenNoodle = "CHICKENNOODLE",
	Dumpling = "DUMPLINGG",
}
